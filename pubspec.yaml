name: beebuckz
description: Bee Buckz — earn rewards for completing tasks.
version: 1.0.0+7
publish_to: none

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  app_tracking_transparency: ^2.0.7
  applovin_max: ^4.6.4
  facebook_app_events: ^0.30.1
  firebase_core: ^4.0.0
  firebase_messaging: ^16.0.0
  flutter:
    sdk: flutter
  webview_flutter: ^4.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
